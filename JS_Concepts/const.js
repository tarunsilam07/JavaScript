const accountId=12345   //can only be assigned by the const keyword

let accountemail="tarun79767@gmail.com"  // variables can be declared by two means one is with let and the other is with var

var accountPassword="1234332"// var is ignoredd should only use let to declare the variables because using var may lead to scope and function errors


accountCity="assam"   // variable can also be declared in this tye but avoid this    
// accountId=22 not allowed

accountCity="slo"

let accstate // if not delcared then it gives as not defined

accountPassword="7373737"

accountemail= "acc@gmail.com"

console.table([accountId,accountPassword,accountemail,accountCity,accstate])