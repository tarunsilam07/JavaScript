if(true)
    {
        let a=1;  // acts as a local variable doesnt make the value go out of the scope acts within the scope
        const b=2   // acts within the scope
        var c=3       // acts outside scope thats why avoid using the var 
    }  
  //  console.log(a);
   // console.log(b);
    console.log(c);